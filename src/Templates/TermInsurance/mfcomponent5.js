import React from "react";

// Shared TailwindCSS class strings
const containerClasses =
  "flex justify-center items-center min-h-screen bg-zinc-100 dark:bg-zinc-900";
const boxClasses =
  "bg-white dark:bg-zinc-800 p-6 max-w-6xl w-full  h-auto md:h-full";
const headerClasses =
  "text-2xl font-semibold mb-6 text-zinc-800 dark:text-white";
const summaryClasses =
  "flex justify-between items-center p-4 cursor-pointer bg-zinc-50 dark:bg-zinc-700 rounded-lg";
const questionClasses =
  "text-lg font-semibold text-zinc-800 dark:text-zinc-200";
const answerClasses = "px-4 pb-4 text-zinc-600 dark:text-zinc-400";

const faqs = [
  {
    question: "What is a Term Insurance & How Does it work?",
    answer: "Term insurance is a type of life insurance policy that provides coverage for a specific period, known as the term Unlike permanent life insurance policies such as whole life or universal life.",
  },
  {
    question: "How to choose a good Term Insurance?",
    answer: "Choosing a good term insurance policy involves careful consideration of several factors to ensure that the policy meets your needs and provides adequate financial protection for your loved ones.",
  },
  {
    question: "How are Term Insurance withdrawals taxed?",
    answer: "Term insurance policies typically do not offer any withdrawal benefits, as they are designed to provide a death benefit to the beneficiaries in the event of the insured's death during the term of the policy",
  },
  {
    question: "How can I invest in Term insurance?",
    answer: "Investing in term insurance involves several steps to select the right policy and purchase coverage that meets your needs.",
  },
];

const FaqItem = ({ question, answer }) => (
  <details className="group">
    <summary className={summaryClasses}>
      <h3 className={questionClasses}>{question}</h3>
      <span className="text-green-500 group-open:hidden">+</span>
      <span className="text-green-500 hidden group-open:inline">−</span>
    </summary>
    <p className={answerClasses}>{answer}</p>
  </details>
);

const TIFaqSection = () => {
  return (
    <div className="px-10">
      <div className={containerClasses}>
        <div className={boxClasses}>
          <h2 className={headerClasses}>Frequently Asked Questions</h2>
          <div className="space-y-4">
            {faqs.map((faq, index) => (
              <FaqItem
                key={index}
                question={faq.question}
                answer={faq.answer}
              />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default TIFaqSection;
